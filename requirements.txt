# =============================================================================
# SECURITY: All dependencies pinned to exact versions (VULN-004)
# Run `pip-audit` and `safety check` before updating any version.
# Last audited: 2026-04-15
# =============================================================================

# Core ML & Data
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.6.0
lifelines==0.29.0            # Survival analysis (Cox PH, Kaplan-Meier)
shap==0.45.1                 # Model interpretability
hdbscan==0.8.39              # Density-based clustering
umap-learn==0.5.6            # Dimensionality reduction
optuna==3.6.1                # Hyperparameter tuning

# Data Generation
faker==22.6.0                # Realistic synthetic data

# Data Infrastructure
# snowflake-connector-python==3.13.1
snowflake-sqlalchemy==1.9.0
python-dotenv==1.0.1
sqlalchemy==1.4.52           # Pinned <2.0 for Snowflake compat
dbt-core==1.7.13             # Data build tool
dbt-snowflake==1.7.3         # dbt adapter for Snowflake

# Azure ML — SDK v1 (deprecated March 2025, EOL June 2026)
# Moved to requirements-azureml-v1.txt to avoid pyOpenSSL<25 conflict.
# Migrate to azure-ai-ml (SDK v2) before EOL.

# MLflow
mlflow==3.11.1

# API & Serving
fastapi==0.135.3
uvicorn[standard]==0.29.0
pydantic==2.11.7
httpx==0.27.0                # Async HTTP client for Streamlit → API calls

# Dashboard
streamlit==1.54.0
plotly==5.22.0
altair==5.3.0

# Orchestration
# apache-airflow==2.8.4

# Monitoring
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==6.1.0

# Security — API auth & rate limiting (VULN-010, VULN-011)
PyJWT==2.12.0
cryptography==46.0.7
pyOpenSSL==26.0.0
slowapi==0.1.9

# Testing & Dev
pytest==9.0.3
ruff==0.4.2

# Security Scanning (dev only)
pip-audit==2.7.3
safety==3.1.0

# LLM & Prescriptive AI
dspy-ai==2.4.9
openai==1.35.10
